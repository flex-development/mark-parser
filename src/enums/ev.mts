/**
 * @file Enums - ev
 * @module fsm-tokenizer/enums/ev
 */

import type { EventType } from '@flex-development/fsm-tokenizer'

/**
 * Event types.
 *
 * @const
 * @enum {EventType}
 */
const enum ev {
  enter = 'enter',
  exit = 'exit'
}

export default ev
