/**
 * @file Fixtures - ct
 * @module fixtures/ct
 */

import type { ContentType } from '@flex-development/mark/parse'

/**
 * Content types.
 *
 * @enum {ContentType}
 */
enum ct {
  content = 'content',
  flow = 'flow',
  document = 'document',
  string = 'string',
  text = 'text'
}

export default ct
